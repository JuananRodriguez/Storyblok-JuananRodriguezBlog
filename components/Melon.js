import React from "react";
import SbEditable from "storyblok-react";

const Melon = ({ blok: { sandia, naranja } }) => {
  return (
    <div>
      <h2>{sandia}</h2>
    </div>
  );
};

export default Melon;
